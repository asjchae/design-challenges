extends layout

block content
    script(src='/javascripts/challengeselect.js')
    each challenge, i in challengepack
        h4 #{challenge.name}
        | Type: #{challenge.type}
        br
        | Prompt #{challenge.prompt}
        br
        | Description #{challenge.description}
        br
        button.challengeselect(value=challenge.name)  Select